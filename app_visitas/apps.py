"""Configuração do aplicativo para app_visitas."""

from django.apps import AppConfig


class AppVisitasConfig(AppConfig):
    """Classe de configuração para o app_visitas."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "app_visitas"
